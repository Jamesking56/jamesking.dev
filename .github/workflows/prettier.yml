name: Lint Prettier

on: [push]

jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - uses: bahmutov/npm-install@v1.8.30
            - name: lint prettier
              run: npm run cstest
              env:
                  CI: true
